<UserControl x:Class="AndroidCat.Controls.SidebarItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AndroidCat.Controls"
             mc:Ignorable="d"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown"
             Loaded="UserControl_Loaded"
             
             Height="30" Width="80">
    <Grid Background="Transparent">
        <Border BorderThickness="2" BorderBrush="Gray" Background="{DynamicResource GridBackground}">
            <Border.CornerRadius>5,5,5,5</Border.CornerRadius>
            <Grid Background="Transparent">
                <TextBlock x:Name="_Label" Text="功能" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
