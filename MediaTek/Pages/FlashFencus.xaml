<Page x:Class="OneCan.MediaTek.Pages.FlashFencus"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OneCan.MediaTek.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="FlashFencus">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid Margin="0,0,5,0">
                
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <GroupBox Header="概览信息">
                    <Grid>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <DataGrid  x:Name="InfoboxD" RowBackground="{DynamicResource GridBackground}" Background="{DynamicResource GridBackground}" AlternatingRowBackground="{DynamicResource GridBackground}" BorderBrush="Gray" BorderThickness="0.5" Foreground="{DynamicResource Foreground}">

                                </DataGrid>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </GroupBox>
            </Grid>
            <Grid Grid.Row="1">
                <GroupBox Header="分区信息">
                    <Grid>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <DataGrid IsReadOnly="True" x:Name="InfoboxDucom" RowBackground="{DynamicResource GridBackground}" Background="{DynamicResource GridBackground}" AlternatingRowBackground="{DynamicResource GridBackground}" BorderBrush="Gray" BorderThickness="0.5" Foreground="{DynamicResource Foreground}">

                                </DataGrid>
                            </Grid>
                        </ScrollViewer>
                    </Grid>
                </GroupBox>
            </Grid>
            <Button Content="刷新" Grid.Row="2"/>
        </Grid>
    </Grid>
</Page>
